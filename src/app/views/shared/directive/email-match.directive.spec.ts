import { EmailMatchDirective } from './email-match.directive';

describe('EmailMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailMatchDirective();
    expect(directive).toBeTruthy();
  });
});
