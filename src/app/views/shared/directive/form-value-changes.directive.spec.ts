import { FormValueChangesDirective } from './FormValueChangesDirective';

describe('FormValueChangesDirective', () => {
  it('should create an instance', () => {
    const directive = new FormValueChangesDirective();
    expect(directive).toBeTruthy();
  });
});
